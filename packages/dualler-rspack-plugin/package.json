{
  "name": "@dualler/rspack-plugin",
  "version": "0.1.0",
  "description": "Dualler Rspack 插件",
  "main": "dist/index.js",
  "scripts": {
    "build": "rslib build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dualler/compiler": "workspace:*"
  },
  "devDependencies": {
    "@rslib/core": "^0.5.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.11.0",
    "@rspack/core": "^1.0.0"
  }
}
